{
  "private": true,
  "workspaces": [
    "packages/*",
    "example/*"
  ],
  "scripts": {
    "boot": "lerna bootstrap",
    "watch": "lerna run watch --parallel",
    "build": "lerna run build --parallel",
    "commit": "lerna publish  --conventional-commits --no-verify-access --force-publish",
    "pub": "lerna publish --no-verify-access"
  },
  "homepage": "https://github.com/ruixijiejie/pi",
  "repository": {
    "type": "git",
    "url": "git@github.com:ruixijiejie/pi.git"
  },
  "bugs": {
    "url": "https://github.com/ruixijiejie/pi/issues"
  },
  "author": "IRIS",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.9.4",
    "lerna": "^3.8.4",
    "tslib": "^2.3.1",
    "tslint": "^5.12.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^4.4.4"
  }
}
